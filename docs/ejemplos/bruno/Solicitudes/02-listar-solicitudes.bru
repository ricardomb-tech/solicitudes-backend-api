meta {
  name: Listar solicitudes (con filtros)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/solicitudes
  body: none
  auth: none
}

params:query {
  estado: recibida
  limit: 10
  offset: 0
}

assert {
  res.status: eq 200
}

docs {
  Filtros combinables: estado, tipo, prioridad, identificador_externo.
  Respuesta paginada: {total, limit, offset, items}.
}
